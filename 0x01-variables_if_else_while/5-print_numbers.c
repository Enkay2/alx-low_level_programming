#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 *  main - Entry point
 *  Return: Always 0 (Success)
 */
int main(void)
{
	int a = 0;
	int z = 9;

	while (a <= z)
	{
		printf("%d"'\n', a);
		a++;
	}
	return (0);
}
