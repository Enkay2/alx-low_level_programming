#include "main.h"

/**
 * _isupper - a function that checks for uppercase character
 * @c: letter is being tested
 * Description: _isupper - prints _putchar message
 * Return: Always 0
 */

int _isupper(int c)

{
	if (c >= 65 && c <= 90)

	{
		return (1);
	}

		return (0);
}

